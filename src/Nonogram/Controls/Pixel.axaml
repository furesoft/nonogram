<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="using:Nonogram.Controls">
    <Design.PreviewWith>
        <controls:Pixel />
    </Design.PreviewWith>

    <Style Selector="controls|Pixel">
        <Setter Property="Template">
            <ControlTemplate>
                <Grid Background="{TemplateBinding Background}" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                    <Label Foreground="White" Background="{TemplateBinding Background}"></Label>
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style Selector="controls|Pixel[IsPointerOver=True]">
        <Setter Property="Background" Value="White" />
    </Style>

    <Style Selector="controls|Pixel[IsActivated=True]">
        <Setter Property="Background" Value="White" />
    </Style>
</Styles>
